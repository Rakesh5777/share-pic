{
  "name": "server",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "dev": "tsc && node dist/index.js",
    "db": "docker run --name postgres -d -e POSTGRES_PASSWORD=postgres -p 5432:5432 postgres",
    "studio": "npx prisma studio",
    "migrate": "npx prisma migrate dev",
    "generate": "npx prisma generate"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^5.19.1",
    "@types/express": "^4.17.21",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "prisma": "^5.19.1"
  }
}
